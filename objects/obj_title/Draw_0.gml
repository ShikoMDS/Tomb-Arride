/// @description Insert description here
// You can write your code in this editor

/* if (FontMenuTitle != -1) 
{
    // Set the text color to RGB values for gold
    draw_set_color(ColGold);

    // Set the font
    draw_set_font(FontMenuTitle);

    // Calculate the x position for center alignment
    var textx = room_width / 2 - string_width("Tomb Arride") / 2;

    // Calculate the y position for center alignment in the top half of the room
    var texty = room_height / 4 - string_height("Tomb Arride") / 2;

    // Draw the text with center alignment
    draw_text(textx, texty, "Tomb Arride");

    // Reset the color to white and font (optimal, can skip)
    //draw_set_color(c_white);
}
